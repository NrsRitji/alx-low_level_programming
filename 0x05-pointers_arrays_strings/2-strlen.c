#include "main.h"
#include <string.h>
/** 
 * _strlen - return the length of a string 
 * @s: char to check 
 * Return: return length of this string
 */

 int _strlen(char *s)
{
	return (strlen(s));
}
